# # OpenCV configuration
# OPENCV_CFLAGS=$(shell pkg-config --cflags opencv4)
# OPENCV_LIBS=$(shell pkg-config --libs opencv4)

# # Adicionando OpenCV às flags de compilação e includes
# FLAGS += $(OPENCV_CFLAGS)
# INCLUDES += $(OPENCV_LIBS)

include $(NEWIFT_DIR)/demo/Makefile
